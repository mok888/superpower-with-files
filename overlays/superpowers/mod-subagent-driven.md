<!-- target: skills/subagent-driven-development/SKILL.md -->
<!-- action: append -->

## Superplanner Memory Integration (Unified Extension)
**CRITICAL THEMATIC RULE:** You are working inside the `superpower-with-files` unified framework. The unified memory architecture dictates that all AI artifacts strictly live in the `.superpower-with-files/` directory.

When the Code Quality Reviewer approves a task, you MUST do the following before moving on:
1. Open `.superpower-with-files/progress.md`.
2. Append the test output directly to the `## Test Results` table in `progress.md`.
3. Update the `Files created/modified` bullet points under the active phase.

Never say "Task Complete" without writing the receipts to `progress.md` first.
