# Ignored Compiled Directories
# We explicitly track skills/, hooks/, and templates/ so end users can pull the repo and use it immediately.
# Only the repo owner needs to run the build_unified.sh script.

# Ignore the build script so end users don't see it
scripts/

# Ignore the AI memory directory
.superpower-with-files/

# Standard Ignores
.DS_Store
node_modules/
*.log
