# Ignored Compiled Directories
# We explicitly track skills/, hooks/, and templates/ so end users can pull the repo and use it immediately.
# Only the repo owner (maintainer) needs to run the build_unified.sh script.

# Ignore all Maintainer-related files (Source, Submodules, and Scripts)
scripts/
MAINTAINER.md
vendor/
overlays/
integration_analysis.md

# Ignore the AI memory directory
.superpower-with-files/

# Standard Ignores
.DS_Store
node_modules/
*.log
